spring.application.name=everything
server.port=8080

# Deshabilitar la página de error de Whitelabel
server.error.whitelabel.enabled=false
# Deshabilitar la página de error de Spring Boot
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.web.ErrorMvcAutoConfiguration

# Swagger
springdoc.swagger-ui.path=/swagger
springdoc.packagesToScan= jrg.everything.controller

# Datos de la base de datos
spring.datasource.url=jdbc:mariadb://localhost:3306/everything
spring.datasource.username=everything
spring.datasource.password=wEqUNA3qTdL
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# Configuración JPA
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect

# Log de SQL para depuración
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true